import { Meta, Story, Canvas, ArgsTable, Source } from '@storybook/addon-docs';
import { Form, InputText, ButtonOutlinePrimary, Wysiwyg } from '../..';
import dedent from 'string-dedent';

<Meta title="Components/Wysiwyg" component={Wysiwyg} />

# Wysiwyg

Note: Wysiwyg has a dependency on react-quill and quilljs. We help with tree shaking by only including this component at `/lib/wysiwyg`
`import { Wysiwyg } from @lambdacurry/component-library/lib/wysiwyg;`

<Canvas>
  <Story
    name="Wysiwyg"
    parameters={{
      docs: {
        source: {
          code: dedent`
          <Wysiwyg formikProps={formikProps} value={formikProps.values.example} name="example" characterLimit={2000} />`
        }
      }
    }}
  >
    <div className="input-form-story">
      <Form initialValues={{ example: '' }} onSubmit={() => {}}>
        {formikProps => (
          <Wysiwyg formikProps={formikProps} value={formikProps.values.example} name="example" characterLimit={2000} />
        )}
      </Form>
    </div>
  </Story>
  <ArgsTable of={Wysiwyg} />
</Canvas>
