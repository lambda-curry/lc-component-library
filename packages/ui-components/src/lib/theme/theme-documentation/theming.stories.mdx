import { Meta, Story, Canvas } from '@storybook/addon-docs';
import classNames from 'classnames';
import dedent from 'string-dedent';

import './theme-story.css';

<Meta title="Theming" component={() => <></>} />

# Theming

This component utilizes MUI https://mui.com/, which requires a theme to be set for its components.
We provided a ThemeBase component that can be utilized a the root of your application to set the theme.

Mixing Tailwind with CSS Variables provides some powerful tools for themeing.

<Canvas>
  <Story
    name="Theme Colors"
    parameters={{
      docs: {
        source: {
          code: dedent`
        {['primary', 'accent'].map(colorName => (
          <li key={colorName}>
            <div className={classNames(\`lc-bg-\${colorName}\`, 'border')} />
            {colorName}
          </li>
        ))}`
        }
      }
    }}
  >
    <div className="theme-story">
      <ul className="lc-flex">
        {['primary', 'accent'].map(colorName => (
          <li key={colorName}>
            <div className={classNames(`lc-bg-${colorName}`, 'border')} />
            {colorName}
          </li>
        ))}
      </ul>
    </div>
  </Story>
</Canvas>

It is easy to change the theme with CSS Variables and classNames.

```css
.alternate-theme {
  --lc-color-primary: red;
  --lc-color-accent: orange;
}
```

<Canvas>
  <Story
    name="Alternate Theme Colors"
    parameters={{
      docs: {
        source: {
          code: dedent`
        {['primary', 'accent'].map(colorName => (
          <li key={colorName}>
            <div className={classNames(\`lc-bg-\${colorName}\`, 'border')} />
            {colorName}
          </li>
        ))}`
        }
      }
    }}
  >
    <div className="theme-story alternate-theme">
      <ul className="lc-flex">
        {['primary', 'accent'].map(colorName => (
          <li key={colorName}>
            <div className={classNames(`lc-bg-${colorName}`, 'border')} />
            {colorName}
          </li>
        ))}
      </ul>
    </div>
  </Story>
</Canvas>
