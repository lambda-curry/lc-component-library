import { Meta, Story, Canvas, ArgsTable, Source } from '@storybook/addon-docs';
import { InputColor, Form, Button } from '../../..';
import dedent from 'string-dedent';

<Meta title="Components/Inputs/Input Color" component={InputColor} />

# Color Input

<Canvas>
  <Story
    name="Input Color"
    parameters={{
      docs: {
        source: {
          code: dedent`
            <Form initialValues={{ color: '#000000' }} onSubmit={() => {}}>
              {formikProps => (
                <>
                  <p>Value: {JSON.stringify(formikProps.values)}</p>
                  <InputColor name="color" />
                </>
              )}
            </Form>`
        }
      }
    }}
  >
    <div className="input-text-story">
      <Form initialValues={{ color: '#000000' }} onSubmit={() => {}}>
        {formikProps => (
          <>
            <p>Value: {JSON.stringify(formikProps.values)}</p>
            <InputColor id="color" name="color" />
          </>
        )}
      </Form>
    </div>
  </Story>
  <ArgsTable of={InputColor} />
</Canvas>
