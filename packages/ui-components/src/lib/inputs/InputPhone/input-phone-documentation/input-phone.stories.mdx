import { Meta, Story, Canvas, ArgsTable, Source } from '@storybook/addon-docs';
import { InputPhone, Form } from '../../..';
import dedent from 'string-dedent';
import './input-phone-story.css';

export const InputPhoneExample = () => (
  <div className="input-text-story">
    <Form initialValues={{ phone: '' }} onSubmit={() => {}}>
      {formikProps => (
        <>
          <p className="lc-mb-16">Values: {JSON.stringify(formikProps.values)}</p>
          <InputPhone name="phone" label="Phone Input" />
        </>
      )}
    </Form>
  </div>
);

<Meta title="Components/Inputs/Input Phone" component={InputPhone} />

# Phone Input

<Canvas>
  <Story
    name="Input Phone"
    parameters={{
      docs: {
        source: {
          code: dedent``
        }
      }
    }}
  >
    {InputPhoneExample.bind({})}
  </Story>
  <ArgsTable of={InputPhone} />
</Canvas>
