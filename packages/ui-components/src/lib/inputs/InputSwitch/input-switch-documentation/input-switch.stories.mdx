import { Meta, Story, Canvas, ArgsTable, Source } from '@storybook/addon-docs';
import { InputSwitch, Form, Button } from '../../..';
import dedent from 'string-dedent';
// import './input-switch-story.css';

<Meta title="Components/Inputs/Input Switch" component={InputSwitch} />

# Input Switch

<Canvas>
  <Story
    name="Input Switch Default"
    parameters={{
      docs: {
        source: {
          code: dedent`
            <h4>Default</h4>
            <Form initialValues={{ switchDefault: false }} onSubmit={() => {}}>
              {formikProps => (
                <>
                  <p>Value: {\`\${formikProps.values.switchDefault}\`}</p>
                  <InputSwitch name="switchDefault" />
                  <Button
                    onClick={() => {
                      formikProps.resetForm();
                    }}
                  >
                    Reset
                  </Button>
                </>
              )}
            </Form>`
        }
      }
    }}
  >
    <div className="input-switch-story">
      <h4>Default</h4>
      <Form initialValues={{ switchDefault: false }} onSubmit={() => {}}>
        {formikProps => (
          <>
            <p>Value: {`${formikProps.values.switchDefault}`}</p>
            <InputSwitch name="switchDefault" />
            <Button
              onClick={() => {
                formikProps.resetForm();
              }}
            >
              Reset
            </Button>
          </>
        )}
      </Form>
    </div>
  </Story>
  <Story
    name="Input Switch Active"
    parameters={{
      docs: {
        source: {
          code: dedent`
            <h4>Active</h4>
            <Form initialValues={{ switchActive: true }} onSubmit={() => {}}>
              {formikProps => <InputSwitch name="switchActive" />}
            </Form>`
        }
      }
    }}
  >
    <div className="input-switch-story">
      <h4>Active</h4>
      <Form initialValues={{ switchActive: true }} onSubmit={() => {}}>
        {formikProps => <InputSwitch name="switchActive" />}
      </Form>
    </div>
  </Story>
  <Story
    name="Input Switch Disabled"
    parameters={{
      docs: {
        source: {
          code: dedent`
            <h4>Disabled</h4>
            <Form initialValues={{ switchActive: true }} onSubmit={() => {}}>
              {formikProps => <InputSwitch name="switchActive" disabled={true} />}
            </Form>`
        }
      }
    }}
  >
    <div className="input-switch-story">
      <h4>Disabled</h4>
      <Form initialValues={{ switchActive: true }} onSubmit={() => {}}>
        {formikProps => <InputSwitch name="switchActive" disabled={true} />}
      </Form>
    </div>
  </Story>
  <Story
    name="Input Switch with Label"
    parameters={{
      docs: {
        source: {
          code: dedent`
            <h4>With Label</h4>
            <Form initialValues={{ switchWithLabelEnd: true, switchWithLabeLStart: false }} onSubmit={() => {}}>
              {formikProps => (
                <>
                  <InputSwitch name="switchWithLabelEnd" label="Click Me" labelPlacement="end" />
                  &nbsp;&nbsp;&nbsp;&nbsp;
                  <InputSwitch name="switchWithLabeLStart" labelOn="On" labelOff="Off" labelPlacement="start" />
                </>
              )}
            </Form>`
        }
      }
    }}
  >
    <div className="input-switch-story">
      <h4>With Label</h4>
      <Form initialValues={{ switchWithLabelEnd: true, switchWithLabeLStart: false }} onSubmit={() => {}}>
        {formikProps => (
          <>
            <InputSwitch name="switchWithLabelEnd" label="Click Me" labelPlacement="end" />
            &nbsp;&nbsp;&nbsp;&nbsp;
            <InputSwitch name="switchWithLabeLStart" labelOn="On" labelOff="Off" labelPlacement="start" />
          </>
        )}
      </Form>
    </div>
  </Story>
  <ArgsTable of={InputSwitch} />
</Canvas>
