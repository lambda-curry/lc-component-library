import { Meta, Story, Canvas, ArgsTable, Source } from '@storybook/addon-docs/blocks';
import { Icon, defaultIcons, DecorativeIcon, ColorIcon, defaultColorIcons, CreditCardBrands, CreditCardIcon } from '..';
import dedent from 'string-dedent';

import './icon-story.css';

<Meta
  title="Icons"
  component={Icon}
  argTypes={{
    name: { control: { disable: true } }
  }}
/>

# Icons

Out of the box we provide plenty of default icons that you can use throughout your app.
However, You can add or replace icons by wrapping your App with the Icon Registry component.

<Canvas>
  <Story
    name="Icon"
    args={{ className: '' }}
    parameters={{
      docs: {
        source: {
          code: dedent`
          {Object.keys(defaultIcons).map(iconName => (
            <li key={iconName}>
              <Icon name={iconName} />
              <br />
              {iconName}
            </li>
          ))}`
        }
      }
    }}
  >
    <div className="icon-story">
      <ul className="icon-story-grid">
        {Object.keys(defaultIcons).map(iconName => {
          console.log(iconName);
          return (
            <li key={iconName}>
              <Icon name={iconName} />
              <br />
              {iconName}
            </li>
          );
        })}
      </ul>
    </div>
  </Story>
  <ArgsTable of={Icon} />
</Canvas>

# Icon Registry

You can add or replace icons by wrapping your App with the Icon Registry component.

```tsx
import { ReactComponent as peaceSymbol } from './hand-peace-light.svg';

export const App = ({ children }) => (
  <IconRegistry icons={{ peaceSymbol }}>
    {children}
  </IconRegistry>
```

Once the above code is implemented correctly, you should be able to use the Icon component to display your new custom icon.

<Canvas>
  <Story
    name="Icon Registry"
    parameters={{
      docs: {
        source: {
          code: dedent`<Icon name="peaceSymbol" viewBox="0 0 448 512" />`
        }
      }
    }}
  >
    <div className="icon-story">
      <Icon name="peaceSymbol" viewBox="0 0 448 512" />
    </div>
  </Story>
</Canvas>

# Decorative Icon

<Canvas>
  <Story
    name="Decorative Icon"
    parameters={{
      docs: {
        source: {
          code: dedent`
        {['black', 'red', 'green', 'blue', '#e30066'].map(colorName => (
          <li key={colorName}>
            <DecorativeIcon name="flame" color={colorName} />
            <br />
            {colorName}
          </li>
        ))}`
        }
      }
    }}
  >
    <div className="icon-story">
      <ul className="icon-story-grid">
        {['black', 'red', 'green', 'blue', '#e30066'].map(colorName => (
          <li key={colorName}>
            <DecorativeIcon name="flame" color={colorName} />
            <br />
            {colorName}
          </li>
        ))}
      </ul>
    </div>
  </Story>
  <ArgsTable of={DecorativeIcon} />
</Canvas>

# Color Icon

<Canvas>
  <Story
    name="Color Icon"
    args={{ className: '' }}
    parameters={{
      docs: {
        source: {
          code: dedent`
          {Object.keys(defaultColorIcons).map(iconName => (
            <li key={iconName}>
              <ColorIcon name={iconName} />
              <br />
              {iconName}
            </li>
          ))}`
        }
      }
    }}
  >
    <div className="icon-story">
      <ul className="icon-story-grid">
        {Object.keys(defaultColorIcons).map(iconName => (
          <li key={iconName}>
            <ColorIcon name={iconName} />
            <br />
            {iconName}
          </li>
        ))}
      </ul>
    </div>
  </Story>
  <ArgsTable of={Icon} />
</Canvas>

# CreditCard Icons

<Canvas>
  <Story
    name="CreditCard Icons"
    parameters={{
      docs: {
        source: {
          code: dedent`
         {['visa', 'mastercard', 'discover', 'jcb', 'dinersclub', 'unknown'].map(brand => (
          <li key={brand}>
            <CreditCardIcon brand={brand} />
            <br />
            {brand}
          </li>
        ))}`
        }
      }
    }}
  >
    <div className="icon-story">
      <ul className="icon-story-grid">
        {['visa', 'mastercard', 'discover', 'jcb', 'dinersclub', 'unknown'].map(brand => (
          <li key={brand}>
            <CreditCardIcon brand={brand} />
            <br />
            {brand}
          </li>
        ))}
      </ul>
    </div>
  </Story>
  <ArgsTable of={CreditCardIcon} />
</Canvas>
