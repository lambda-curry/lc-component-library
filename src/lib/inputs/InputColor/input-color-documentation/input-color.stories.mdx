import { Meta, Story, Canvas, ArgsTable, Source } from '@storybook/addon-docs/blocks';
import { InputColor, Form } from '../../..';
import dedent from 'string-dedent';

<Meta title="Input Color" component={InputColor} />

# Color Input

<Canvas>
  <Story
    name="Input Date"
    parameters={{
      docs: {
        source: {
          code: dedent`
            <Form
              initialValues={{
                color: '#000000'
              }}
              onSubmit={() => {}}
            >
              {formikProps => (
                <>
                  <InputColor name="color" formikProps={formikProps} />
                </>
              )}
            </Form>`
        }
      }
    }}
  >
    <div className="input-text-story">
      <Form
        initialValues={{
          color: '#000000'
        }}
        onSubmit={() => {}}
      >
        {formikProps => (
          <>
            <InputColor id="color" name="color" formikProps={formikProps} />
          </>
        )}
      </Form>
    </div>
  </Story>
  <ArgsTable of={InputColor} />
</Canvas>
