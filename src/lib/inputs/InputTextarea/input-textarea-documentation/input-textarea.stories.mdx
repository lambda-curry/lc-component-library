import { Meta, Story, Canvas, ArgsTable, Source } from '@storybook/addon-docs/blocks';
import { InputTextarea, Form } from '../../..';
import dedent from 'string-dedent';
import './input-textarea-story.scss';

<Meta title="Input Textarea" component={InputTextarea} />

# Textarea Input

<Canvas>
  <Story
    name="Input Textarea"
    parameters={{
      docs: {
        source: {
          code: dedent`
            <Form
              initialValues={{
                sampleInput1: '',
              }}
              onSubmit={() => {}}
            >
              {formikProps => (
                <>
                  <InputTextarea name="sampleInput1" label="Textarea Input" formikProps={formikProps} />
                </>
              )}
            </Form>`
        }
      }
    }}
  >
    <div className="input-text-story">
      <Form
        initialValues={{
          sampleInput1: ''
        }}
        onSubmit={() => {}}
      >
        {formikProps => (
          <>
            <InputTextarea name="sampleInput1" label="Textarea Input" formikProps={formikProps} characterLimit={50} />
          </>
        )}
      </Form>
    </div>
  </Story>
  <ArgsTable of={InputTextarea} />
</Canvas>
