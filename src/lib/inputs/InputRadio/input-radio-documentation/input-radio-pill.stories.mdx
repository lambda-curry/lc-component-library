import { Meta, Story, Canvas, ArgsTable, Source } from '@storybook/addon-docs/blocks';
import { InputRadio, InputRadioPill, Form, Button } from '../../..';
import dedent from 'string-dedent';

<Meta title="Input Radio Pill" component={InputRadioPill} />

# Input Radio Pill

<Canvas>
  <Story
    name="Input Radio Pill"
    parameters={{
      docs: {
        source: {
          code: dedent`
          <h4>Default</h4>
          <Form enableReinitialize={true} initialValues={{ radioDefault: '' }} onSubmit={() => {}}>
            {formikProps => (
              <>
                <p>Value: {\`\${formikProps.values.radioDefault}\`}</p>
                <InputRadio name="radioDefault" label="Default radio" formikProps={formikProps} />
                <Button
                  onClick={() => {
                    formikProps.resetForm();
                  }}
                >
                  Reset
                </Button>
              </>
            )}
          </Form>`
        }
      }
    }}
  >
    <div className="input-radio-story">
      <h4>Default</h4>
      <Form enableReinitialize={true} initialValues={{ radioDefault: '' }} onSubmit={() => {}}>
        {formikProps => (
          <>
            <p>Value: {`${formikProps.values.radioDefault}`}</p>
            <InputRadioPill
              name="radioDefault"
              inputs={[
                { id: 1, label: 'One', value: 'one' },
                { id: 2, label: 'Two', value: 'two' },
                { id: 3, label: 'Three', value: 'three' }
              ]}
              formikProps={formikProps}
            />
            <Button
              onClick={() => {
                formikProps.resetForm();
              }}
            >
              Reset
            </Button>
          </>
        )}
      </Form>
    </div>
  </Story>
  <ArgsTable of={InputRadioPill} />
</Canvas>
