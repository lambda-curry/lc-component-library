import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import classNames from 'classnames';
import dedent from 'string-dedent';

import './theme-story.css';

<Meta title="Theming" component={() => <></>} />

# Theming

Mixing Tailwind with CSS Variables provides some powerful tools for themeing.

<Canvas>
  <Story
    name="Theme Colors"
    parameters={{
      docs: {
        source: {
          code: dedent`
        {['primary', 'accent'].map(colorName => (
          <li key={colorName}>
            <div className={classNames(\`lc-bg-\${colorName}\`, 'border')} />
            {colorName}
          </li>
        ))}`
        }
      }
    }}
  >
    <div className="theme-story">
      <ul className="lc-flex">
        {['primary', 'accent'].map(colorName => (
          <li key={colorName}>
            <div className={classNames(`lc-bg-${colorName}`, 'border')} />
            {colorName}
          </li>
        ))}
      </ul>
    </div>
  </Story>
</Canvas>

It is easy to change the theme with CSS Variables and classNames.

```css
.alternate-theme {
  --lc-color-primary: red;
  --lc-color-accent: orange;
}
```

<Canvas>
  <Story
    name="Alternate Theme Colors"
    parameters={{
      docs: {
        source: {
          code: dedent`
        {['primary', 'accent'].map(colorName => (
          <li key={colorName}>
            <div className={classNames(\`lc-bg-\${colorName}\`, 'border')} />
            {colorName}
          </li>
        ))}`
        }
      }
    }}
  >
    <div className="theme-story alternate-theme">
      <ul className="lc-flex">
        {['primary', 'accent'].map(colorName => (
          <li key={colorName}>
            <div className={classNames(`lc-bg-${colorName}`, 'border')} />
            {colorName}
          </li>
        ))}
      </ul>
    </div>
  </Story>
</Canvas>
