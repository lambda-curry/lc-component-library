import { Meta, Story, Canvas, ArgsTable, Source } from '@storybook/addon-docs';
import dedent from 'string-dedent';

import { ImageCropper } from '../ImageCropper';
import { ImageCropperExample, ImageCropperExampleString } from './image-cropper.examples.tsx';

<Meta title="Image Cropper" component={ImageCropper} />

# Image Cropper

<Canvas>
  <Story
    name="Image Cropper"
    parameters={{
      docs: {
        source: {
          code: dedent(ImageCropperExampleString)
        }
      }
    }}
  >
    {ImageCropperExample.bind({})}
  </Story>
  <ArgsTable of={ImageCropper} />
</Canvas>
