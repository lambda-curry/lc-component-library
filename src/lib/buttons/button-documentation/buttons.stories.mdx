import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import {
  Button,
  ButtonPrimary,
  ButtonAccent,
  ButtonSuccess,
  ButtonWarning,
  ButtonDanger,
  ButtonOutline,
  ButtonOutlinePrimary,
  ButtonOutlineAccent,
  ButtonOutlineSuccess,
  ButtonOutlineWarning,
  ButtonOutlineDanger,
  ColorIcon,
  Icon
} from '../..';
import {
  ButtonExamples1,
  ButtonExamples2,
  ButtonExamples3,
  ButtonExamples4,
  ButtonExamples5
} from './buttons.examples.tsx';
import dedent from 'string-dedent';
import './button-story.css';

<Meta title="Components/Buttons/Buttons" component={Button} />

# Buttons

<Canvas>
  <Story
    name="Buttons"
    parameters={{
      docs: {
        source: {
          code: dedent`
          <Button>Default Button</Button>
          <ButtonPrimary>Primary Button</ButtonPrimary>`
        }
      }
    }}
  >
    {ButtonExamples1.bind({})}
  </Story>
  <Story
    name="Disabled Buttons"
    parameters={{
      docs: {
        source: {
          code: dedent`
          <Button disabled>Default Button</Button>
          <ButtonPrimary disabled={true}>Primary Button</ButtonPrimary>`
        }
      }
    }}
  >
    {ButtonExamples2.bind({})}
  </Story>
  <ArgsTable of={Button} />
</Canvas>

## Other button types

<Canvas>
  <Story
    name="Buttons with Icons"
    parameters={{
      docs: {
        source: {
          code: dedent`
          <Button icon={<ColorIcon name="googleCalendar" />}>Default Button</Button>
          <ButtonOutline icon={<Icon name="settings" />} iconPlacement="end">
            Outline Button
          </ButtonOutline>`
        }
      }
    }}
  >
    {ButtonExamples3.bind({})}
  </Story>
  <Story
    name="Buttons as another component"
    parameters={{
      docs: {
        source: {
          code: dedent`
          <ButtonLink as={buttonProps => <a {...buttonProps} href="#test" />}> Anchor Tag Button</ButtonLink>
          <ButtonUnstyled>Unstyled Button</ButtonUnstyled>`
        }
      }
    }}
  >
    {ButtonExamples4.bind({})}
  </Story>
</Canvas>

## Button Groups

<Canvas>
  <Story
    name="Buttons Groups"
    parameters={{
      docs: {
        source: {
          code: dedent`
    const [state, toggle] = useReducer<(state: { [x: string]: boolean }, action: string) => { [x: string]: boolean }>(
      (state, action) => {
        const newState = { ...state };
        newState[action] = !state[action];
        return newState;
      },
      {}
    );
    return (
    <p>{JSON.stringify(state)}</p>
    <ButtonGroup>
      <ButtonPrimary className={classNames({ active: state.left })} onClick={() => toggle('left')}>
        Left
      </ButtonPrimary>
      <ButtonPrimary className={classNames({ active: state.middle })} onClick={() => toggle('middle')}>
        Middle
      </ButtonPrimary>
      <ButtonPrimary className={classNames({ active: state.right })} onClick={() => toggle('right')}>
        Right
      </ButtonPrimary>
    </ButtonGroup>)`
        }
      }
    }}
  >
    {ButtonExamples5.bind({})}
  </Story>
</Canvas>
