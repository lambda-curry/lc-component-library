import { useState } from 'react';
import { Meta, Story, Canvas, ArgsTable, Source } from '@storybook/addon-docs/blocks';
import { Form, Slider, TimeRangeSlider } from '../..';
import dedent from 'string-dedent';

<Meta title="Slider" component={Slider} />

# Slider

<Canvas>
  <Story
    name="Slider"
    parameters={{
      docs: {
        source: {
          code: dedent`
            <Form
              initialValues={{
                sliderValue: [20, 27]
              }}
              onSubmit={() => {}}
            >
              {formikProps => (
                <>
                  <p>Value: {JSON.stringify(formikProps.values.sliderValue)}</p>
                  <Slider name="sliderValue" min={0} max={100} label="Slider" formikProps={formikProps} />
                </>
              )}
            </Form>`
        }
      }
    }}
  >
    <div className="slider-story">
      <Form
        initialValues={{
          sliderValue: [20, 27]
        }}
        onSubmit={() => {}}
      >
        {formikProps => (
          <>
            <p>Value: {JSON.stringify(formikProps.values.sliderValue)}</p>
            <Slider name="sliderValue" min={0} max={100} label="Example Slider" formikProps={formikProps} />
          </>
        )}
      </Form>
    </div>
  </Story>
  <ArgsTable of={Slider} />
</Canvas>

# Time Range Slider

<Canvas>
  <Story
    name="Time Range Slider"
    parameters={{
      docs: {
        source: {
          code: dedent`
            <Form
              initialValues={{
                sliderValue: ['09:00', '17:00']
              }}
              onSubmit={() => {}}
            >
              {formikProps => (
                <>
                  <p>Value: {JSON.stringify(formikProps.values.sliderValue)}</p>
                  <TimeRangeSlider name="sliderValue" label="Time Range Slider" formikProps={formikProps} />
                </>
              )}
            </Form>`
        }
      }
    }}
  >
    <div className="slider-story">
      <Form
        initialValues={{
          sliderValue: ['09:00', '17:00']
        }}
        onSubmit={() => {}}
      >
        {formikProps => (
          <>
            <p>Value: {JSON.stringify(formikProps.values.sliderValue)}</p>
            <TimeRangeSlider name="sliderValue" label="Time Range Slider" formikProps={formikProps} />
          </>
        )}
      </Form>
    </div>
  </Story>
  <ArgsTable of={Slider} />
</Canvas>
