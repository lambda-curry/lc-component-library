import { Meta, Story, Canvas, ArgsTable, Source } from '@storybook/addon-docs';
import dedent from 'string-dedent';

import { FileUploader } from '../FileUploader';
import { FileUploaderWithImageCropper } from '../FileUploaderWithImageCropper';
import {
  FileUploaderExample,
  FileUploaderExampleString,
  FileUploaderMultipleExample,
  FileUploaderMultipleExampleString,
  FileUploaderWithImageCropperExample,
  FileUploaderWithImageCropperExampleString
} from './file-uploader.examples.tsx';

<Meta title="Modules/File Uploader" component={FileUploader} />

# File Uploader

## Single File

<Canvas>
  <Story
    name="Single File"
    parameters={{
      docs: {
        source: {
          code: dedent(FileUploaderExampleString)
        }
      }
    }}
  >
    {FileUploaderExample.bind({})}
  </Story>
  <ArgsTable of={FileUploader} />
</Canvas>

## Multiple Files

<Canvas>
  <Story
    name="Multiple Files"
    parameters={{
      docs: {
        source: {
          code: dedent(FileUploaderMultipleExampleString)
        }
      }
    }}
  >
    {FileUploaderMultipleExample.bind({})}
  </Story>
  <ArgsTable of={FileUploader} />
</Canvas>

## With Image Cropper

<Canvas>
  <Story
    name="File Uploader with Image Cropper"
    parameters={{
      docs: {
        source: {
          code: dedent(FileUploaderWithImageCropperExampleString)
        }
      }
    }}
  >
    {FileUploaderWithImageCropperExample.bind({})}
  </Story>
  <ArgsTable of={FileUploaderWithImageCropper} />
</Canvas>
